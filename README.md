After knitting and rendering `caribouNN.Rmd`, overwrite this file with a symlink to `caribouNN.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/Tati/GitHub/caribouNN
rm README.md && ln -s caribouNN.md README.md
```
